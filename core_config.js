const { ThreeJSMap, GuidesLoader, NeuroAd } = require("neuro-core/components")

const config = {
  components: [ThreeJSMap, GuidesLoader, NeuroAd],
  streaming_id: 10,
  device_started_at: "9:00",
  device_off_on: "20:00"
}

module.exports = config
